fn get_help_text() {
    // return help text content
}
fn get_character_map() {
    // provide character array, color definitions
    //  include cursor, shade on active step
}

fn tick() {
    // read from sequence queue (next event)
    // build midi content
    //  parse event details
    // return midi event content
}

fn init(tick_rate) {
    // initialize sequencer queue
    //  random (first iteration)
}

fn destroy() {
    //
}
